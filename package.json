{
  "name": "nuxt-capacitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build-android": "nuxt build && nuxt generate && npx cap copy android && cd android && ./gradlew assembleDebug && cd ..",
    "build-only-android": "npx cap copy android && cd android && ./gradlew assembleDebug && cd .."
  },
  "dependencies": {
    "@capacitor/android": "^2.4.7",
    "@capacitor/cli": "^2.4.7",
    "@capacitor/core": "^2.4.7",
    "@nuxtjs/axios": "^5.13.1",
    "@nuxtjs/pwa": "^3.3.5",
    "animejs": "^3.2.1",
    "core-js": "^3.9.1",
    "howler": "^2.2.1",
    "lodash": "^4.17.21",
    "nuxt": "^2.15.7"
  },
  "devDependencies": {}
}
